{
  "name": "melodia",
  "version": "1.0.0",
  "description": "Melodia — AI Music SaaS powered by TTAPI Suno API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:worker": "wrangler dev",
    "deploy": "wrangler deploy",
    "tunnel": "cloudflared tunnel --config cloudflare/config.yml run"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "wrangler": "^4.11.1"
  },
  "engines": {
    "node": ">=18"
  }
}
